---
interface Props {
	isoDate: string;
}

const { isoDate } = Astro.props;
const date = isoDate ? new Date(isoDate) : new Date();
---

{
	date.toLocaleDateString('en-us', {
		year: 'numeric',
		month: 'short',
		day: 'numeric',
	})
}
